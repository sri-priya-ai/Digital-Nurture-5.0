Setup:
npm create vite@latest student-portal-react -- --template react
cd student-portal-react
npm install

Then replace the generated src/App.jsx with this folder's App.jsx,
and copy courseData.js + components/ into src/.

Run:
npm run dev
